[package]
name = "agentdeck-protocol"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "Shared types and wire format for AgentDeck daemon-app communication"
license = "GPL-3.0-or-later"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
